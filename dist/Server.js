"use strict";
exports.__esModule = true;
var App_1 = require("./App");
var HookRouter_1 = require("./routes/HookRouter");
var app = new App_1["default"]();
app.initialize();
app.addRouter(new HookRouter_1["default"]());
var port = parseInt(process.argv[2], 10);
app.start(port);
exports["default"] = app;
